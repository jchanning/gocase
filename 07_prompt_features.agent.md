1. Create a user interface to create and edit tests. it should make it easy to set the question, provide 4 possible answers and label one answer as correct.
2. Implement validation to ensure that the test data is correctly formatted before saving it to the database.
3. Add functionality to assign tests to specific subjects, difficulty levels, and standards.
4. Add the ability to upload an image with the question.
5. Implement a preview feature that allows teachers to see how the test will appear to students before publishing it.
6. Add a user Admin screen which is only displayed to Admin users. 
7. The User Admin screen should display a table showing all users currently set up. 
8. The Admin should be able to create new users, setting their email, username, password and role (Student, Teacher, Admin).
9. The Admin should be able to delete user accounts
10. Ensure that only Admin users can create Teacher and Admin accounts during user registration.